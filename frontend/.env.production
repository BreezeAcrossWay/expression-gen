# 生产环境API配置
# 部署到Render后，将下面的URL替换为你的后端URL
VITE_API_URL=https://your-backend-url.onrender.com

# 部署步骤：
# 1. 先部署后端到Render
# 2. 获取后端URL（例如：https://sticker-backend-abc123.onrender.com）
# 3. 把上面的URL替换成你的后端URL
# 4. git add . && git commit -m "更新API地址" && git push
# 5. 重新部署前端
