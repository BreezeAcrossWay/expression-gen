# 🚀 快速开始指南

## ✅ 服务已启动成功！

你的明星表情包生成器已经成功部署并运行！

### 📍 访问地址

- **前端界面**: http://localhost:3000
- **后端API**: http://localhost:5000
- **API健康检查**: http://localhost:5000/api/health

### 🎯 使用步骤

1. **打开浏览器访问**: http://localhost:3000
2. **上传明星照片**: 点击上传区域或拖拽照片
3. **选择卡通风格**: 从4种风格中选择喜欢的一个
4. **生成表情包**: 自动生成开心、难过、思考三种表情
5. **自定义表情**: 输入"生气"、"大笑"等提示词生成更多
6. **导出下载**: 单个导出或一键打包下载全部

### 📁 项目结构

```
/workspace/
├── backend/          # 后端服务 (Flask)
│   ├── app.py       # 主应用文件
│   ├── uploads/     # 上传图片目录
│   └── outputs/     # 生成图片目录
├── frontend/        # 前端应用 (Vue)
│   └── src/
│       └── App.vue  # 主界面组件
└── README.md        # 完整文档
```

### 🛠️ 管理命令

#### 查看服务状态
```bash
ps aux | grep -E "(python3 app.py|vite)" | grep -v grep
```

#### 查看后端日志
```bash
tail -f /workspace/backend/backend.log
```

#### 查看前端日志
```bash
tail -f /workspace/frontend/frontend.log
```

#### 停止服务
```bash
# 停止后端
pkill -f "python3 app.py"

# 停止前端
pkill -f "vite"
```

#### 重启服务
```bash
# 重启后端
cd /workspace/backend && python3 app.py > backend.log 2>&1 &

# 重启前端
cd /workspace/frontend && npm run dev -- --host 0.0.0.0 > frontend.log 2>&1 &
```

### 🎨 功能特性

✅ **智能上传**: 支持JPG、PNG、GIF等格式，最大16MB
✅ **多风格生成**: 4种卡通风格自动生成
✅ **表情包制作**: 开心、难过、思考等基础表情
✅ **自定义提示**: 输入任意表情描述生成个性化表情
✅ **灵活管理**: 随时添加或删除表情包
✅ **便捷导出**: 单个下载或ZIP打包下载

### 🔧 配置AI API（可选）

如需使用真实的AI图像生成功能：

1. 编辑 `/workspace/backend/.env` 文件
2. 添加你的API密钥：
   ```
   STABILITY_API_KEY=your-api-key-here
   ```
3. 重启后端服务

### 📚 完整文档

更多详细信息请查看: `/workspace/README.md`

### ❓ 常见问题

**Q: 前端页面无法访问？**
A: 确保服务正在运行，检查3000端口是否被占用

**Q: API请求失败？**
A: 检查后端日志，确认5000端口服务正常

**Q: 如何添加更多表情？**
A: 在界面中输入自定义提示词，如"委屈"、"惊讶"等

### 🎉 开始使用

现在就访问 http://localhost:3000 开始制作你的专属表情包吧！

---

**祝你使用愉快！** 🎭
